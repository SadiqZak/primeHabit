import { extendTheme } from "@chakra-ui/react"

const colors = {
    navbar: '#20232A',
    sidebar: '#282C34',
    primaryColor: '#61DAFB',
    white: "#FFFF",
}

export const theme = extendTheme({colors})